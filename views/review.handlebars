
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Leave A Review</title>
 {{! BOOTSTRAP LINK}}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
<link rel="stylesheet" href="../public/css/styles.css">    {{! LINKED STYLES SHEET IN PUBLIC FOLDER }}
</head>


<body class="h-100 text-bg-dark">
    <header>

        {{!-- NAVBAR --}}
        {{> navbar}}
  
        </header>

    <container class="text-center mt-3">   

        {{! REVIEWS HEADER }}

        <h1>How Did Your Date Go?</h1>

        <h2>Leave A Review And Select An HTTP Status Badge For User:</h2>

    </container>

 <main class="cover-container d-flex  w-75 p-3 mx-auto flex-column">

    {{! REVIEW FORM}}
    <section class="review-form">

            {{! USER ABLE TO WRITE REVIEW }}
                <div class="mb-3">

                <label for="Review_text" class="form-label">How Was Your Experience?</label>

                <textarea class="form-control" id="Review_text"></textarea>

                </div>

                {{! HTTP STATUS BADGE SELECTION }}  
                <div class="mb-3">

                <label for="reviewBadge" class="form-label">Choose A Status Code Badge:</label>

                <select class="form-select" id="reviewBadge">

                {{#each statusCodes}}

                <option value="{{this.code}}">{{this.name}}</option> {{! had to add this before code and name }}

                {{/each}}

                </select>

                </div>

                {{! SUBMIT BUTTON }}
                <a href="/submitReviews" class="btn btn-lg btn-light fw-bold border-white bg-white">Submit</a>
                <a href="/submitReviews" class="btn btn-primary submitBtn">Submit HTTP Status Badge</button>

            
        </section>
        
    </main>






</body>

</html>